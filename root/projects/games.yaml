apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: games
spec:
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  destinations:
    - namespace: asa
      name: "in-cluster"
  sourceRepos:
    - "*"
